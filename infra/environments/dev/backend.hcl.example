# Copy to backend.hcl (git-ignored) after the bootstrap bucket exists.

bucket       = "retailops-ai-tfstate-000000000000"
key          = "retailops-ai/dev/terraform.tfstate"
region       = "us-east-1"
encrypt      = true
use_lockfile = true
